// Declara o módulo models — Rust precisa saber de cada módulo explicitamente
// É como import/require, mas declarativo
mod models;

// Ponto de entrada do backend — será expandido nas próximas tasks
fn main() {
    println!("PORT:3000");
}
